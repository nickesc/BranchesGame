using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class yachtScript : MonoBehaviour
{
    /*public GameObject yacht;
    private MeshRenderer yachtMesh;
    private bool visible;

    // Start is called before the first frame update
    void Start()
    {
        yachtMesh = yacht.GetComponent<MeshRenderer>();
        yachtMesh.enabled = false;
        visible = false;
    }

    // Update is called once per frame
    void Update()
    {
        if (visible == false)
        {
            if (wokeUpTrig == false)
            {
                
            }
        }
        
    }*/
}
